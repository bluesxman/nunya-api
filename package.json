{
  "name": "nunya-api",
  "version": "1.0.0",
  "description": "Secure file transfer API Lambda backend",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@types/aws-lambda": "^8.10.0",
    "aws-sdk": "^2.1000.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}